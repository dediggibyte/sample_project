# .github/workflows/deploy.yml
name: Build and Deploy Databricks Bundle

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy-bundle:
    name: Validate and Deploy Bundle to Dev
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Databricks CLI (v2 Public Preview with bundle)
        run: |
          # Download & install the standalone CLI binary (v0.205+)
          curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | sudo sh

      - name: Verify Databricks CLI Installation
        run: |
          echo "Which databricks binary?"
          which databricks
          echo "Databricks CLI version:"
          databricks --version
          echo "Bundle commands available:"
          databricks bundle --help

      - name: Validate Databricks Bundle for Dev target
        run: databricks bundle validate
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}

      - name: Deploy Databricks Bundle to Dev target
        run: databricks bundle deploy -t dev
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
